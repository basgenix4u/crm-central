package com.crmcentral.config;

import org.springframework.context.annotation.Configuration;

/**
 * WebSocket configuration - disabled for initial deployment.
 * Enable when WebSocket support is needed.
 */
@Configuration
public class WebSocketConfig {
    // WebSocket disabled for memory optimization on free tier
}
